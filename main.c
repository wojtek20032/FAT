#include <stdio.h>
#include <stdint.h>
#include "file_reader.h"
#include <stdlib.h>


int main() {
//    struct disk_t* disk = disk_open_from_file("../0001natural_fat12_volume.img");
//    struct volume_t* volume = fat_open(disk, 0);
//    struct dir_t* pdir = dir_open(volume, "\\");
//    for (int i = 0; i < 12; ++i) {
//        struct dir_entry_t entry;
//        int res = dir_read(pdir, &entry);
//        printf("%s res: %d\n", entry.name, res);
//    }
    return 0;
}

